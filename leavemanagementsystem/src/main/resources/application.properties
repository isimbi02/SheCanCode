spring.application.name=leavemanagementsystem
server.port=${LEAVE_SERVICE_PORT:8080}

# PostgreSQL datasource (overridable via env)
spring.datasource.url=${DB_URL:jdbc:postgresql://postgres:5432/shecancode}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.jpa.hibernate.ddl-auto=update

# JWT secret and expiration (overridable via env)
app.jwt.secret=${JWT_SECRET:ZmFrZVN1cGVyU2VjcmV0S2V5Rm9yRGVtby0xMjM0NTY3ODkw}
app.jwt.expMinutes=${JWT_EXP_MINUTES:240}
